<%= form_with(model: user_tweet) do |form| %>
  <% if user_tweet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_tweet.errors.count, "error") %> prohibited this user_tweet from being saved:</h2>

      <ul>
        <% user_tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
