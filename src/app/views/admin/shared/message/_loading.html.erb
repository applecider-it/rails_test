<style>
/* 中央に表示 */
.app-admin-loading-placeholder {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 10rem;
  font-family: sans-serif;
  font-size: 1.2rem;
  color: #555;
}
/* ローディングアニメーション */
.app-admin-loading-spinner {
  width: 2rem;
  height: 2rem;
  border: 3px solid #ccc;
  border-top-color: #333;
  border-radius: 50%;
  animation: app-admin-loading-spin 1s linear infinite;
  margin-right: 0.5rem;
}
@keyframes app-admin-loading-spin {
  0% {
    transform: rotate(0deg);
  }

  100% {
    transform: rotate(360deg);
  }
}
</style>
<div class="app-admin-loading-placeholder">
    <div class="app-admin-loading-spinner"></div>
    <div>読み込み中…</div>
</div>